int main()
{
    // 변수 이름은 마음대로
    // 알파벳 대소문자, _, 숫자(첫글자 X)
    // 특수문자 X, +, - 등 X, 예약어 X
    int num_apples;
    int my_love;
    int house;
    int cracker;
    int a;
    int A;
    // int a; 중복 X
    // int tax rate; 빈칸 비허용

    // int x; // declaration
    // int y;
    // int z;
    int x = 1, y = 2, z; // 같은 자료형일 때, 가능

    // x = 1; // assignment
    // y = 2;
    z = x + y;
    return 0;
}